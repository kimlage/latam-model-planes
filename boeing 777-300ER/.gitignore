# As fotos de referencia desta pasta (refs/, incluindo refs/thumbs/) e a folha
# comparacao_fin_sash.png sao cobertas pela regra unica do .gitignore da raiz.
# Nao repita a regra aqui: se uma foto escapar, corrija na raiz.

# Varreduras e arrays intermediarios da extracao (reproduziveis com
# extract_77w.py a partir do PDF do APR).
_*.npy
_*.png
z_*.png
zf_*.png
g_*.png
m_*.png
fin_rect*.png

# Documento dimensional do fabricante (gratuito no site da Boeing, direitos
# reservados) e recortes derivados dele — mesma regra do B787_APR na raiz.
B777_APR_boeing.pdf
p*_thumb.png
apr*.png
insp_*.png
