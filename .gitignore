# ---------------------------------------------------------------------------
# Material de terceiros que NAO pode ser redistribuido sob a licenca do projeto.
# Veja NOTICE.md para as fontes e como obter cada item.
# ---------------------------------------------------------------------------

# Documentos dimensionais dos fabricantes (Airbus ACAP / Boeing APR).
# Sao gratuitos no site do fabricante, mas com direitos reservados.
A320_ACAP_airbus.pdf
**/B787_APR_boeing.pdf

# Rasterizacoes e recortes derivados desses documentos
acap*.png
apr*.png
**/apr*.png
insp_*.png
**/insp_*.png

# Fotos de referencia de terceiros (sem metadados de autoria/licenca registrados)
ref_*.jpg
**/ref_*.jpg
comparacao_com_foto.png
**/comparacao_com_foto.png

# Vetores de marca registrada (LATAM, Airbus, Boeing)
latam_logo_indigo.svg
airbus_a320neo_logo.svg
dreamliner_logo.svg

# ---------------------------------------------------------------------------
# Arquivos de trabalho
# ---------------------------------------------------------------------------
*.blend1
*.blend2
debug_*.png
**/A320neo_voo.blend
.DS_Store
__pycache__/
*.pyc

# ---------------------------------------------------------------------------
# Cenario SCL: raw elevation tiles (~1.5 GB). Reproduce with cenario/fetch_dem.sh
# The derived heightfields under cenario/terreno/ ARE committed.
# ---------------------------------------------------------------------------
cenario/dem/
cenario/dem_cop/
cenario/dem_cop_hgt/
cenario/dem_srtm_clean/
cenario/terreno/_fine_profile.npy

# ---------------------------------------------------------------------------
# SCL scenario: third-party reference photographs. They are properly credited in
# cenario/scl_referencias.md, but they are CC BY-SA — share-alike conflicts with
# this repo's CC BY 4.0 asset licence, so we cite them instead of shipping them.
# Same rule as the aircraft reference photos. Overlays built on map tiles too.
# ---------------------------------------------------------------------------
cenario/refs/*.jpg
cenario/refs/*.jpeg
cenario/refs/_mapa_*.png
